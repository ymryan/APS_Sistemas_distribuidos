// src/config/firebase.js
// Firebase desativado por enquanto.
// Para ativar futuramente: descomente o código abaixo, instale o pacote firebase
// e preencha as credenciais do seu projeto no console.firebase.google.com

// import { initializeApp } from 'firebase/app';
// import { getAuth } from 'firebase/auth';
// import { getFirestore } from 'firebase/firestore';
// const firebaseConfig = { apiKey: "...", authDomain: "...", ... };
// const app = initializeApp(firebaseConfig);
// export const auth = getAuth(app);
// export const db = getFirestore(app);

export const auth = null;
export const db = null;
export default null;
